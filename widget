import 'package:flutter/material.dart';

void main() {
  runApp(const BiodataApp());
}

class BiodataApp extends StatelessWidget {
  const BiodataApp({super.key});

  @override
  Widget build(BuildContext context) {
    return MaterialApp(
      title: 'Biodata App',
      debugShowCheckedModeBanner: false,
      home: const BiodataScreen(),
    );
  }
}

class BiodataScreen extends StatelessWidget {
  const BiodataScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      backgroundColor: Colors.lightGreen[100],
      appBar: AppBar(
        title: const Text('Biodata Diri'),
        backgroundColor: const Color.fromARGB(255, 4, 0, 255),
      ),
      body: Center(
        child: Container(
          padding: const EdgeInsets.all(25),
          margin: const EdgeInsets.all(20),
          decoration: BoxDecoration(
            color: Colors.white,
            borderRadius: BorderRadius.circular(25),
            boxShadow: [
              BoxShadow(
                color: Colors.black26,
                blurRadius: 8,
                offset: const Offset(2, 4),
              ),
            ],
          ),
          child: Column(
            mainAxisSize: MainAxisSize.min,
            children: [
              // FOTO PROFIL (CIRCLE AVATAR)
              const CircleAvatar(
                radius: 60,
                backgroundImage: NetworkImage(
                  'https://cdn.discordapp.com/attachments/1346452454908362753/1426607581111259197/Gambar_WhatsApp_2025-10-11_pukul_20.21.56_7d6eaad4.jpg?ex=68ebd758&is=68ea85d8&hm=a835508a505c32825001d8a95fd146dc3d1d728dff8b08af0c92d6ee6f161634',
                ),
              ),
              const SizedBox(height: 20),

              // NAMA LENGKAP
              const Text(
                'Ahmad azqi yauci ',
                style: TextStyle(
                  fontSize: 22,
                  fontWeight: FontWeight.bold,
                  color: Color.fromARGB(255, 8, 11, 216),
                ),
                textAlign: TextAlign.center,
              ),
              const SizedBox(height: 10),

              // DESKRIPSI SINGKAT
              const Text(
                'Mahasiswa sistem informasi yang menyukai pemrograman, '
                'membangun aplikasi, dan terus belajar hal baru di dunia digital.',
                style: TextStyle(fontSize: 16, color: Colors.black87),
                textAlign: TextAlign.center,
              ),
              const SizedBox(height: 20),

              // TOMBOL AKSI
              ElevatedButton(
                onPressed: () {
                  // Menampilkan pesan snackbar
                  ScaffoldMessenger.of(context).showSnackBar(
                    const SnackBar(
                      content: Text('Halo! Ini adalah biodata saya 😄'),
                      duration: Duration(seconds: 4),
                    ),
                  );

                  // Menampilkan pesan di konsol
                  debugPrint('Tombol ditekan! Halo dari Ahmad azqi yauci  👋');

                },
                style: ElevatedButton.styleFrom(
                  backgroundColor: const Color.fromARGB(255, 10, 29, 199),
                  padding:
                      const EdgeInsets.symmetric(horizontal: 30, vertical: 12),
                  shape: RoundedRectangleBorder(
                    borderRadius: BorderRadius.circular(20),
                  ),
                ),
                child: const Text(
                  'Tampilkan Pesan',
                  style: TextStyle(fontSize: 16, color: Colors.white),
                ),
              ),
            ],
          ),
        ),
      ),
    );
  }
}
